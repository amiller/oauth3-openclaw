# Staging — tee.oauth3-stage.monerolink.com
DOMAIN=tee.oauth3-stage.monerolink.com
CORS_ORIGIN=https://oauth3-stage.monerolink.com
GATEWAY_DOMAIN=_.dstack-pha-prod9.phala.network

# Cloudflare DNS (edit zone for monerolink.com)
CLOUDFLARE_API_TOKEN=

# Shared with orchestrator — generate: openssl rand -hex 32
JWT_SECRET=
PG_PASSWORD=

# Optional
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
CERTBOT_EMAIL=amiller@oauth3.net
