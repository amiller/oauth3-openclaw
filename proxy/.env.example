# Server
PORT=3737
PUBLIC_URL=http://localhost:3737

# Database (SQLite for local dev)
DB_PATH=./proxy.db
# DATABASE_URL=postgres://user:pass@localhost:5432/oauth3  # for production

# Auth
JWT_SECRET=change-me-to-something-random

# LLM policy drafting (optional — permits still work without it, you just write specs by hand)
ANTHROPIC_API_KEY=

# CORS
CORS_ORIGIN=*
